package UnitIndexer_config

// Local imports:
import LocalObjectIDs

@config public function shouldIndex(unit _u) returns boolean
	return _u.isType(UNIT_TYPE_PEON) or _u.getTypeId() == UNIT_RACE_PICKER
